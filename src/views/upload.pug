extends base.pug


block content 
    if errorMessage     
        span=errorMessage
    form(method="POST") 
            input(type="text", name="title" placeholder="Title"  required,maxlength=10)
            input(type="text", name="description" placeholder="Description" required , minlength=10)
            input(type="text", name="hashtags" placeholder="Hashtags, separated by comma" required)
            input(type="submit", value="upload video")