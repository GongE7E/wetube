mixin video(video)
    div
            h4
                a(href=`/videos/${video.id}`)=video.title
            ul  
                p=video.description
                ul
                    each hashtags in video.hashtags
                        li=hashtags 
                small=video.createdAt  