extends ../base.pug

block content 

    form(method="post") 
        input(placeholder="Old Password" type="password" name="Old Password", required)
        input(placeholder="New Password" type="password", name="New Password", required )
        input(placeholder="New Password Confirmation" type="password", name="New Password Confirmation"  required)
        input(type="submit", value="Change Password")